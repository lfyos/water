uniform int primitive_type;

layout (location = 0) out vec4 frag_data;

void main(void)
{
	frag_data=vec4(0.3,0.3,0.3,1.0);
	
	if(primitive_type>0)
		gl_FragDepth=-0.9999;
	else{
		gl_FragDepth=gl_FragCoord.z;
		vec2 p=gl_PointCoord-vec2(0.5,0.5);
		if(dot(p,p)>0.25)
			discard;
	}
}
