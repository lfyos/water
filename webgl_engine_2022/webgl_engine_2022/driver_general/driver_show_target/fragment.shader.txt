
uniform sampler2D texture_sampler;
uniform float color_scale;

in vec4 frag_texture;

layout (location = 0) out vec4 frag_data;

void main(void)
{
	frag_data=vec4(texture(texture_sampler,frag_texture.xy).rgb*color_scale,1.0);
}