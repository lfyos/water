uniform sampler2D texture_sampler;

layout (location = 0) out vec4 frag_data;

void main(void)
{
	vec2 p=gl_PointCoord-vec2(0.5,0.5);
	if(dot(p,p)>0.25){
		discard;
		return;
	}
	frag_data=texture(texture_sampler,gl_PointCoord);
	
	frag_data.w=dot(frag_data.rgb,frag_data.rgb);
	
	return;
}
