uniform sampler2D texture_sampler;

in vec4 frag_texture;

layout (location = 0) out vec4 frag_data;

void main(void)
{
	frag_data=texture(texture_sampler,frag_texture.xy);
	if(frag_data.a<0.1)
		discard;
}
